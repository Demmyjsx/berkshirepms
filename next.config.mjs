/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    turbopack: false,
  },
  // You can keep your other config options here if you have any
};

export default nextConfig;
